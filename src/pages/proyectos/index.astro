---
import Layout from '../../layouts/main/Layout.astro';
import { ProjectList } from '../../components/ProjectList';

// Opcionalmente podrías pre-cargar datos para SSR
// const apiUrl = import.meta.env.API_URL || 'http://localhost:4000/api';
// let initialProyectos = [];
// try {
//   const response = await fetch(`${apiUrl}/proyectos`);
//   initialProyectos = await response.json();
// } catch (error) {
//   console.error('Error al cargar proyectos:', error);
// }
---

<Layout title="Proyectos">
  <!-- No necesitamos pasar onSelectProject ni onCreateProject porque 
       ahora se manejan internamente con window.location -->
  <ProjectList client:load />
</Layout>