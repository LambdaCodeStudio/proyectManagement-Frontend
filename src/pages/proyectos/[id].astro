---
import Layout from '../../layouts/main/Layout.astro';
import { ProjectDetail } from '../../components/ProjectDetail';

// Obtener el ID del proyecto desde los parámetros de ruta
const { id } = Astro.params;

// Opcionalmente podrías precargar el proyecto para SEO
// const apiUrl = import.meta.env.API_URL || 'http://localhost:4000/api';
// let proyecto;
// try {
//   const response = await fetch(`${apiUrl}/proyectos/${id}`);
//   proyecto = await response.json();
// } catch (error) {
//   console.error('Error al cargar proyecto:', error);
// }
---

<Layout title="Detalle de Proyecto">
  <!-- Solo necesitamos pasar el ID, el resto se maneja internamente -->
  <ProjectDetail id={id} client:load />
</Layout>